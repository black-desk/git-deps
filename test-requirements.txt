# Test dependencies
# Note: This project now uses pyproject.toml for dependency management.
# This file is maintained for backward compatibility.
# For the authoritative dependency list, see pyproject.toml [project.optional-dependencies]

# Add requirements only needed for your unittests and during development here.
# They will be installed automatically when running `pip install -e .[test]`.
# ATTENTION: Don't remove pytest-cov and pytest as they are needed.
pytest-cov
pytest
